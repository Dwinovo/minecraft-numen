# goal-2 tasks

## 执行规则

- 每轮只执行第一个未完成 task。
- 每个 task 完成后记录实现、验证、剩余风险、提交和下一步。
- 每完成三个 task 执行一次大型全面检查-debug 循环。
- 前端改动必须保留真实客户端截图证据，并实际读图确认。

## Task 1：修复服务端单参数同伴路由

状态：`[ ] 未开始`

目标：当 `/goal <word>` 精确匹配已拥有同伴名时，只向该同伴发送 `/goal status`；不匹配时保留 `/goal <内容>` 直接创建并广播给已拥有同伴的语义。

验证：覆盖精确匹配、不匹配、带后续命令、无已拥有同伴；运行相关单测与服务端安全检查。

完成记录：

剩余风险：

下一步：Task 2。

## Task 2：为未知 goal 子命令增加拼写保护

状态：`[ ] 未开始`

目标：对与已知子命令高度接近的首词返回明确纠错提示，同时保留自然语言和普通单词的直接创建语义。

验证：覆盖 `comlete`/`udpate` 等 typo、中文目标、多词自然语言、真实未知单词和全部已知命令。

完成记录：

剩余风险：

下一步：Task 3。

## Task 3：闭环 FAILED 生产状态

状态：`[ ] 未开始`

目标：API/LLM 现有重试最终耗尽时，把 active goal 标记 FAILED 并持久化可见错误；`/goal resume` 能恢复执行，非 API 中断不误标。

验证：覆盖瞬时重试成功、最终失败、无 goal、paused/completed、FAILED 恢复。

完成记录：

剩余风险：

下一步：第 1 轮大型检查。

## 第 1 轮大型检查（Task 1-3 后）

状态：`[ ] 未执行`

检查：需求偏离、命令兼容、状态迁移、持久化、线程约束、安全、测试、构建、diff 和文档。

检查记录：

## Task 4：外接大脑释放后自动续跑 goal

状态：`[ ] 未开始`

目标：实体级 external control 或全局 MCP 模式释放后，active goal 自动且幂等地补排执行；接管期间不启动内置大脑。

验证：覆盖 acquire/add/release、MCP 开关、重复释放、paused/terminal/dead 状态和已有队列。

完成记录：

剩余风险：

下一步：Task 5。

## Task 5：修复需要参数命令的 Enter/Tab 补全

状态：`[ ] 未开始`

目标：`/goal add`、`/goal update` 等候选完成后追加空格并继续输入，不直接提交；无参数命令和普通聊天行为保持不变。

验证：纯策略测试、ChatInputBar 编译、真实输入框键盘操作和截图。

完成记录：

剩余风险：

下一步：Task 6。

## Task 6：拒绝 null GoalState 静默丢失

状态：`[ ] 未开始`

目标：`GoalCommands.execute` 明确拒绝 null，并确保调用点与测试都传入持久化实体状态。

验证：null fail-fast 测试、调用点审查、全部 goal 命令回归。

完成记录：

剩余风险：

下一步：第 2 轮大型检查。

## 第 2 轮大型检查（Task 4-6 后）

状态：`[ ] 未执行`

检查：恢复幂等性、队列重复、输入行为回归、异常契约、API/Fabric/NeoForge 构建、文档和回滚。

检查记录：

## Task 7：修复人格切换后的错误积压状态

状态：`[ ] 未开始`

目标：owner prompt 与 ambient event 的 backlog 语义分离；切换人格仍记录 reconciliation event，但状态 Tab 不再显示错误“积压 0 条/积压信息”。

验证：覆盖 persona-change event、真实 prompt 积压、事件保留与下一回合消费；编译客户端。

完成记录：

剩余风险：

下一步：Task 8。

## Task 8：状态 Tab 与补全 UI 真实视觉验证

状态：`[ ] 未开始`

目标：在实际客户端分别验证人格切换前后、真实 prompt 积压、长人格名，以及需要参数命令的补全状态；保存前后截图并读图。

验证：截图路径、视口/状态、关键 DOM 等价可见信息与独立读图结论写入本文件。

完成记录：

剩余风险：

下一步：Task 9。

## Task 9：全量构建、兼容审查与交付物

状态：`[ ] 未开始`

目标：运行目标模块测试/构建，审查主仓库与 1.21.10 交付范围，生成所需 jar，确认没有覆盖用户改动。

验证：Gradle 输出、`git diff --check`、jar 内容/时间戳和工作树审查。

完成记录：

剩余风险：

下一步：第 3 轮大型检查与最终 review。

## 第 3 轮大型检查（Task 7-9 后）

状态：`[ ] 未执行`

检查：UI/UX、截图、测试、构建、安全、数据一致性、交付包、文档、回滚和全部 7 项需求证据。

检查记录：

## Task 10：最终 review 与 goal 完成审计

状态：`[ ] 未开始`

目标：逐项对照 6 个遗留问题和人格切换积压问题，检查代码、测试、运行时、截图、构建和交付物证据；修复所有已知高风险问题后才标记完成。

验证：形成逐项完成矩阵；只有 7 项均有直接证据时才更新 goal 为 complete。

完成记录：

## 最终状态

目标完成：`[ ] 未完成`

最终 review 记录：

标记 goal 完成：`[ ] 未完成`
