package com.dwinovo.numen.core.pathing.astar;

/**
 * 目的格哈希 → 成本系数表:上一条路径上的动作打折(默认 0.5),
 * 重规划时倾向沿旧路走,抑制路径抖动。
 */
public final class Favoring {
    // TODO: 规格书 C3 节 —— Long2DoubleOpenHashMap 表与 calculate(hash)
}
